{
	"version": 2,
	"builds": [{ "src": "/index.ts", "use": "@vercel/node" }],
	"routes": [{ "src": "/(.*)", "dest": "/" }],
	"git": {
		"deploymentEnabled": {
			"development": false
		}
	},
	"functions": {
		"app/controllers/authController.ts": {
			"includeFiles": "db_creation/*.sql"
		}
	}
}
